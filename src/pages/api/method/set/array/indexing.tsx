import React from 'react';

const ArrayIndexingApiPage : React.FC<{className: string}> = ({ className }) => (
    <article className={ `array-indexing-api-page ${ className }` }>
        <h1>Updating at a Specific Array Index</h1>
        <div>
            
        </div>
    </article>
);

export default ArrayIndexingApiPage;