import React from 'react';

const IndexingApiPage : React.FC<{className: string}> = ({ className }) => (
    <article className={ `indexing-api-page ${ className }` }>
    </article>
);

export default IndexingApiPage;