import type { HeadFC } from 'gatsby';

import React from 'react';

export { default } from '../../partials/contents/history_features-page';

export const Head : HeadFC = () => (
    <meta
        content="What\'s changed?"
        name="description"
    />
);