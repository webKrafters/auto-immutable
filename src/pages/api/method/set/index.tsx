import React from 'react';

const SetMethodApiPage : React.FC<{className: string}> = ({ className }) => (
    <article className={ `set-method-api-page ${ className }` }>
    </article>
);

export default SetMethodApiPage;