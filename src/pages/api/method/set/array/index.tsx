import React from 'react';

const SetArrayApiPage : React.FC<{className: string}> = ({ className }) => (
    <article className={ `set-array-api-page ${ className }` }>
        <h1>Working with Arrays</h1>
        <div>
            
        </div>
    </article>
);

export default SetArrayApiPage;